// use crate::application::{service::errors::PostServiceError, state::AppState};

pub struct PostService ;

impl PostService {

//     // todo: impl the image attachment
//     pub async fn create_post(
//         state: &AppState,
//         user_id: i64,
//         content: &str,
//         status:  &str,
//         // ? image: &str  
//     ) -> Result<i64, PostServiceError> {
//         let mut tx = state.db_pool.begin().await?;
        
//         let post_id = state.snowflake_generator.generate_id()?;

//         Ok(post_id)
//     }

    // pub async fn update_post(
    //     state: &AppState,
    //     id: i64,
    //     user_id: i64,
    //     content: &str
    // ) -> Result<(), PostServiceError> {
    //     let mut tx = state.db_pool.begin().await?;

    // }
}